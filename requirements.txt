# =============================================================================
# requirements.txt — Neonatal Sepsis API
# =============================================================================
# PYTHON VERSION: 3.11.x  (set in runtime.txt)
#
# WHY THESE VERSIONS:
#   - All packages below have pre-built wheels for Python 3.11 on Linux x86_64
#     so Render NEVER compiles from source (avoids Cython/meson/gcc errors)
#   - scikit-learn MUST stay at 1.6.1 to match pkl training environment
#   - pandas 2.2.2 has official cp311 wheels on PyPI — no source build needed
# =============================================================================

# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
gunicorn==23.0.0

# Data — pandas 2.2.2 has cp311-cp311-manylinux wheel on PyPI
numpy==1.26.4
pandas==2.2.2

# ML — MUST stay at 1.6.1 to match pkl training environment
scikit-learn==1.6.1
joblib==1.4.2

# XGBoost
xgboost==2.1.3

# Pydantic v2
pydantic==2.10.4
